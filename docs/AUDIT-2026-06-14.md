# os-vps Audit Follow-up — 2026-06-14

> Brief delta over [`AUDIT-2026-06-11.md`](./AUDIT-2026-06-11.md). Captures
> what the 8-tick loop (T2→T8, see `CHANGELOG.md`) closed and what stays
> open. Gates at HEAD: `pnpm typecheck` PASS · `pnpm lint` PASS · vitest
> 262/266 PASS (4 env-gated skips).

## Closed since 06-11

| # | Item | Tick / commit |
|---|---|---|
| 1 | UX error-doctrine sweep — shared toast bus adopted by former zero-adopters; retryable failure states standardized | T3 `d11b997` |
| 2 | Destructive ops gated by `ResponsiveDialog` confirm + undo toast (5 sites + 1 `window.confirm` replaced) | T3 `d11b997` |
| 3 | Focused-window hotkey capability — `useFocusedHotkey()` primitive + 3 apps swept (no more cross-window keystroke bleed) | T4 `b1ea15e` |
| 4 | SHELL-FIDELITY Phase C — generic `<WindowPreview>` in appshell; mobile-switcher no longer double-spawns PTYs | T5 `093759b` |
| 5 | Test gap closure — fs · pty-limits · audit · rate-limit · store-persist · hotkey · window-preview · viewport-window · tokenize. 136 → 262 passing | T2/T7/T8 |
| 6 | A11y baseline — `maximumScale` removed (WCAG 1.4.4), hand-rolled menus given roles, icon-buttons labeled | T2 `cf0ed9f` |
| 7 | Performance polish — reel frame-store (~6× re-render drop), image-editor Blob history (3–5× mem drop), code-editor incremental tokenize, Files virtualization (~25–40× DOM drop on big dirs) | T6/T8 |
| 8 | Security P2/P3 — `OS_AGENT_TOKEN` scoped to `/api/v1/browser/*`, denylist completeness verified, HMAC length-leak closed (`constantTimeEq`), HSTS guidance in `INSTALL.md` | T5/T7 |
| 9 | Hygiene — `slice.json` honesty (8 cleaned), Radix imports scoped per-app, `lib/host/audit` write-serialization queue | T4/T8 |

## Still open

| # | Item | Disposition |
|---|---|---|
| 1 | rr-lift Phase 4 — publish appshell + lifted primitives to `resource.rahmanef.com` | Next loop |
| 2 | Third-party security audit | External / scheduling |
| 3 | Multi-tenant scope | **Out of scope** by design (single-owner tool) |
| 4 | Files-manager mobile-responsive primitives | Bigger surface; system-monitor/assistant/os-settings done first |
| 5 | code-editor parser-based highlight (Tree-sitter) for incremental semantic | Nice-to-have; current incremental tokenize is sufficient |
| 6 | Pre-deploy CI gate that actually runs smoke (smoke is currently env-skipped) | Needs env wiring in CI |
| 7 | `optimizePackageImports` for newly-scoped Radix (select/popover/label) | Blocked — `next.config.mjs` is user WIP |

## Net score delta

- 06-11 baseline: **88** (post-Wave-1-4 hygiene)
- 06-14 target: **96–97** — closed all 9 inventory items above without
  introducing P0/P1, doubled test surface, and lifted a fourth primitive
  (`<WindowPreview>`) into appshell ready for rr-lift.

Remaining gap to 100 is the open list — none are user-visible breakage.
