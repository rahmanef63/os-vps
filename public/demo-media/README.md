# Demo media

Drop image / video / audio / pdf files here, then regenerate the manifest:

    node scripts/gen-demo-media.mjs

They show up under the **"Demo Media"** root in the Files app and open in the
media viewer — even in the public demo (served as static bytes, no host/auth).
