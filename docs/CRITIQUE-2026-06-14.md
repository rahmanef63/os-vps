# MSO — QA-ego Critique + Builder-ego Backlog (2026-06-14)

> Followup to `AUDIT-2026-06-11.md`. Method: read docs/code, spot-verify prior
> P0/P1 fixes, score 1-100 via weighted rubric. Score baseline: **85/100** at
> `f9e1980`. Targets the deferred items + new gaps.

## Score 85/100 — sub-breakdown

| Dim | Score | Weight | Notes |
|---|---:|---:|---|
| Architecture | 92 | 0.18 | Slice discipline tight, appshell→rr-liftable convergence done, 200-line rule honored |
| Security | 90 | 0.20 | Auth/jail clean, hardening wave done. Sisa P2/P3 (agent-token scope, length-leak, HSTS doc) |
| Code Quality | 88 | 0.12 | 0 `any`, 0 ts-ignore, 162 tests, eslint clean |
| Testing | 75 | 0.14 | Gap: `lib/host/fs.ts`, `device-store.ts`, UrlSync dedup, store `multi`/`focusApp` |
| Performance | 80 | 0.10 | Pollers gated. Sisa: reel re-render, code-editor tokenize, Files no virtualization |
| A11y | 70 | 0.08 | `maximumScale:1` fails WCAG 1.4.4; hand-rolled menus/tabs no roles |
| DX/Docs | 92 | 0.08 | Exemplary. 8 Convex-era `slice.json` stale |
| Deployment | 88 | 0.10 | systemd prod+demo solid. No pre-deploy E2E gate |

`Overall = 85.4`

## Backlog (top 6, ROI-ordered)

| # | Task | Expected lift |
|---|---|---:|
| 1 | Focused-window hotkey capability + sweep 6 apps | +4 |
| 2 | `<WindowPreview>` primitive (Phase C) — kills mobile-switcher double-PTY | +3 |
| 3 | Test gap fill — fs/auth/UrlSync/store contracts | +3 |
| 4 | A11y quick wins — drop `maximumScale:1`, label icon-buttons, role menus | +3 |
| 5 | Error doctrine pass 1 — toast-bus adoption (3 surfaces) | +2 |
| 6 | Confirm/undo via ResponsiveDialog — 4 destructive ops | +2 |

Expected total: +17 pts → caps 100.

## Long-term (not blocking 100)

- MOBILE-RESPONSIVE-PLAN primitive adoption cross-app
- Lift to rr (Phase 4)
- Reel/code-editor re-render hotspots, Files virtualization
- 8 Convex-era `slice.json` cleanup
- Radix monolith vs scoped import alignment

## Verification rule (per CLAUDE.md)

Reproduce on demo `:4006` first → fix → `pnpm typecheck` + `pnpm build` →
`sudo systemctl restart os-vps.service`. Never the reverse.
