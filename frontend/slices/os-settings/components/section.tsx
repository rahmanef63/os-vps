// Re-export the portable shell-settings building block under the local name the
// app-specific sections (ai/server/about) already use. Body lives in shell-settings.
export { SettingsSection as Section } from "@/features/shell-settings";
