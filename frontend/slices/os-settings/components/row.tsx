// Re-export the portable shell-settings building block under the local name the
// app-specific sections (ai/server) already use. Body lives in shell-settings.
export { SettingsRow as Row } from "@/features/shell-settings";
